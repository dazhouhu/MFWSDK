namespace MFW.Core
{
    public enum CallMode
    {
        AUDIOVIDEO_CALL = 0     /**< Initiates call mode is audio-video call. */
        ,AUDIO_CALL             /**< Initiates call mode is audio call. */
    }
}
